# Der Default-Pfad (Gemini REST + Strategie + Speicherung + Telegram) läuft rein
# mit der Python-Standardbibliothek — KEINE Pakete nötig.
#
# Optionale Abhängigkeiten je nach gewähltem Backend:

# Nur für --backend gemini_sdk (alternativ zum REST-Default):
# google-generativeai>=0.7.0

# Nur für --backend cloud_vision (Composio / Google Cloud Vision):
# google-cloud-vision>=3.4.0
# composio_core>=0.5.0

# Entwicklung/Tests:
pytest>=7.0
